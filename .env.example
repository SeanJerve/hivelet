# =============================================================================
# Hivelet - Environment Template
# Copy to .env at the repository root and fill in the real values.
# .env is gitignored; never commit it.
#
# THIS FILE MUST CONTAIN PLACEHOLDERS ONLY. NEVER A REAL VALUE.
#
# On 2026-09-13 this template was found to contain the LIVE Supabase
# service_role key, the live anon key, and the live JWT signing secret. They had
# been committed since 2026-08-25. All three must be treated as compromised and
# rotated; removing them from this file does not remove them from git history.
# =============================================================================

# Server Configuration
PORT=5000
NODE_ENV=development

# Supabase Configuration
# Project URL is not a secret, but the keys are. Find them in:
#   Supabase Dashboard -> Project Settings -> API
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here
# service_role BYPASSES row-level security. It can read and write every row in
# every table. It is a server-only secret - it must never reach a browser, a
# frontend build, a screenshot, or this file.
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# MySQL Database Configuration (legacy - the system runs on Supabase PostgreSQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=hivelet_db

# JWT Security Credentials
# Every access token in the system is signed with this. Anyone who knows it can
# mint a token claiming any role, which defeats the entire 39-permission RBAC
# model regardless of how correct that model is.
# Generate a fresh one with:
#   node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
JWT_SECRET=generate_a_random_48_byte_secret_do_not_use_a_phrase
JWT_EXPIRES_IN=7d

# Adyen GCash Payment Sandbox Credentials (Optional Feature)
# Leaving these as mock_ placeholders is a supported configuration:
# adyenService.isLiveConfigured() rejects any value starting with "mock_" and the
# system falls back to the local settlement path. That is the pluggable adapter
# working as designed, not a misconfiguration.
ADYEN_API_KEY=mock_adyen_api_key_sandbox
ADYEN_MERCHANT_ACCOUNT=mock_merchant_account
ADYEN_ENVIRONMENT=TEST

# CORS - website/ dev server runs on 5174, frontend/ wireframe basis on 5173
CLIENT_URL=http://localhost:5174
CORS_ORIGINS=http://localhost:5173,http://localhost:5174
